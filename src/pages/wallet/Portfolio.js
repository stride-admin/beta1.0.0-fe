import React, { useState } from 'react';
import { useAppContext } from '../../AppContext';

export default function Portfolio() {
    return (
        <div className='portfolio'>
            <p>I'll be honest, the only user with access to this is me coz I don't know how to configure the db stack for asset management so you can enjoy the rest of the app for now 😁</p>
        </div>
    );
}